extends layout

block content
  h1= title

  if error
    p Error getting counts
  else
    ul
      li #[a(href='/catalog/goods') Goods] (!{data.goodsCount})
      li #[a(href='/catalog/genres') Genres] (!{data.genreCount})
      li #[a(href='/catalog/works') Works] (!{data.workCount})
